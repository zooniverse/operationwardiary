<% if @groups?: %> 
  <label>
    Change diary
    <select id="diary_picker">
      <% for group in @groups: %>
        <option value="<%= group.id %>"><%= group.name %></option>
      <% end %>
    </select>
  </label>
<% end %>