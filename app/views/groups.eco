<% translate = require 't7e' %>

<% if @groups?: %>
  <ul>
    <% for group in @groups: %>
    <% console.log group %>
    <li>
      <a href="#/classify/<%= group.id %>"><%= group.name %></a>
      (<%= group.startdate %> &ndash; <%= group.enddate %>)
    </li>
    <% end %>
  </ul>
<% end %>