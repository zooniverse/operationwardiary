<% translate = require 't7e' %>
<% labels = require '../lib/notes' %>

<button name="toggle"></button>
<select>
  <% for activity in labels.activities: %>
    <option value="<%= activity %>"><%- translate 'span', "activities.#{activity}" %></option>
  <% end %>
</select>